use crate::structs::App;
use color_eyre::eyre::Result;
use ratatui::crossterm::event::Event;

impl App {
    pub fn main_menu_load_playthrough_handle_event(&self, event: Event) -> Result<()> {
        todo!();
    }
}
